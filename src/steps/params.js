const config = [
    {
        name: 'folder title',
        type: 'folder',
        children: [
            { name: 'text', type: 'string', path: 'text' },
        ],
    },
];

export default config;
